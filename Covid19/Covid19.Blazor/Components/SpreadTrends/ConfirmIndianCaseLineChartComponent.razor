
<div class="container">
    <div class="row">
        <div class="twelve columns">
            @if (ItemSource != null)
            {
            <div class="rounded shadow">
                <h4>Confirmed Indian</h4>
                <RadzenChart ColorScheme="ColorScheme.Divergent">
                    <RadzenLineSeries Smooth="false"
                                      Data="@ItemSource"
                                      CategoryProperty="day"
                                      Title="Indian"
                                      LineType="LineType.Solid"
                                      ValueProperty="confirmedCasesIndian"
                                      TItem="RegionalSTModel">
                        <RadzenMarkers MarkerType="MarkerType.Square" />
                    </RadzenLineSeries>
                    <RadzenCategoryAxis Padding="20" FormatString="{0:MMM}">
                        <RadzenTicks Stroke="green" StrokeWidth="2" />
                    </RadzenCategoryAxis>
                    <RadzenValueAxis Formatter="@FormatAsConfirmIndianCase">
                        <RadzenGridLines Visible="true" />
                        <RadzenAxisTitle Text="Confirmed Indian" />
                    </RadzenValueAxis>

                </RadzenChart>
            </div>
               
            }
            else
            {
                <Alert Color="Color.Warning" Visible="true">
                    <AlertMessage>No Record Found</AlertMessage>
                </Alert>
            }
        </div>
    </div>
</div>

@code {
    [Parameter]
    public List<RegionalSTModel> ItemSource { get; set; }

    string FormatAsConfirmIndianCase(object value)
    {
        return Convert.ToString(value);
    }
}
